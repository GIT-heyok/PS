#include <bits/stdc++.h>

#define FAST                     \
    ios::sync_with_stdio(false); \
    cin.tie(nullptr);            \
    cout.tie(nullptr);
using namespace std;
#define endl '\n'

/*
 * skill issued...
 */
int main(){
    FAST
    cout<<2000<<endl;
    for (int i = 0; i < 1000; ++i) {
        cout<<1<<" ";
    }
    for (int i = 0; i < 1000; ++i) {
        cout<<1000<<" ";
    }
    cout<<endl;
}